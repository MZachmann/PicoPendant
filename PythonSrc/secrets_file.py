# This file is where you keep secret settings, passwords, and tokens!
# When the application starts it reads the wlan settings from here
#   and then uses any config.json settings file (serialized GlobalPico) to override them
#
secrets = {
    'wlan_ssid' : 'myssid',
    'wlan_password' : 'mypass',
    }
